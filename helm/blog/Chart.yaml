# ---------------------------------------------------------------------------
# GENERATED FILE — managed by flake-hub (golden-argocd).
# Do not edit manually: `nix run .#generate` overwrites it.
# To change it, edit repo.nix, or the template in the pack that owns it.
# ---------------------------------------------------------------------------

apiVersion: v2
# The -chart suffix keeps the chart's ECR repository distinct from the image's.
# `helm push` takes the repository name from here, so the suffix has to be part
# of the chart's own name rather than something the publish workflow appends.
name: blog-chart
description: A Helm chart for blog
type: application
version: 0.3.0
appVersion: "0.3.0"
